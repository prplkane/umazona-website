# .gitignore — privacy-first, repo-root for Umazon
# Protect secrets and local-only files before pushing to GitHub.

# ------------------------
# Node / package managers
# ------------------------
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ------------------------
# Build outputs / caches
# ------------------------
dist/
build/
out/
.cache/
.parcel-cache/
.next/
.vite/
public/build/

# Frontend-specific (React/Vite/Next)
frontend/node_modules/
frontend/build/
frontend/dist/
frontend/.cache/
frontend/out/
frontend/.parcel-cache/
frontend/.next/

# Backend-specific
backend/node_modules/

# ------------------------
# Uploaded files, binaries, and large artifacts
# ------------------------
# Ignore everything in backend/uploads (user uploads, processed CSVs, etc.)
# but keep a .gitkeep so the directory can stay in the repo if needed.
backend/uploads/**
!backend/uploads/.gitkeep

# ------------------------
# Environment, secrets, keys (privacy-sensitive)
# ------------------------
# Ignore env files and local overrides
.env
.env.*.local
.env.local
.env.development
.env.test
.env.production

# Ignore private keys and certs
*.pem
*.key
*.p12
*.crt
*.csr

# ------------------------
# Databases, caches, logs
# ------------------------
*.sqlite
*.sqlite3
*.db
*.db-journal
logs/
*.log
*.log.*

# ------------------------
# IDEs, editors, OS files
# ------------------------
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.iml
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# ------------------------
# Misc / temp
# ------------------------
*.tgz
*.zip
*.tmp
*.temp
*.swp
*~
.#*
jest-results.json
coverage-final.json
*.tsbuildinfo

# ------------------------
# Safety: keep lockfiles and example envs tracked
# ------------------------
!/.env.example
# Do NOT ignore lockfiles — keep them in the repo to ensure reproducible installs
!package-lock.json
!yarn.lock

# ------------------------
# Notes and tips
# ------------------------
# 1) This file intentionally ignores secrets (.env, keys, certs) and uploads.
# 2) If you already committed secrets accidentally, they remain in git history
#    — consider removing them with `git rm --cached <file>` and then using
#    the BFG Repo-Cleaner or `git filter-repo` to purge history before pushing.
# 3) If you want a per-project .gitignore (e.g., `frontend/.gitignore`), we can
#    extract frontend/backend-specific lines into those files instead of the root.

